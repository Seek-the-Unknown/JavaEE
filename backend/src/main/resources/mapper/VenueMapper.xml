<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.VenueMapper">

    <select id="findById" resultType="com.entity.Venue">
        SELECT * FROM t_venue WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.entity.Venue">
        SELECT * FROM t_venue
        <where>
            is_active = 1
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="findByOwnerId" resultType="com.entity.Venue">
        SELECT * FROM t_venue
        WHERE owner_id = #{ownerId} AND is_active = 1
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_venue (name, type, description, capacity, price_per_hour, image_url, is_active, owner_id)
        VALUES (#{name}, #{type}, #{description}, #{capacity}, #{pricePerHour}, #{imageUrl}, 1, #{ownerId})
    </insert>

    <update id="update">
        UPDATE t_venue
        SET name = #{name}, type = #{type}, description = #{description},
            capacity = #{capacity}, price_per_hour = #{pricePerHour},
            image_url = #{imageUrl}, is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE t_venue SET is_active = 0 WHERE id = #{id}
    </update>

</mapper>